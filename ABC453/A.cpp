#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n;
    cin >> n;
    string s;
    cin >> s;

    while(s.size() > 0 && s[0] == 'o')
    {
        s.erase(s.begin());
    }
    // cout << s << "\n";
}

int main()
{
    solve();
    return 0;
}